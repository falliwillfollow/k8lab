# overprovisioned

Excessive requests can waste schedulable capacity or leave pods Pending. This overlay uses very high CPU requests so roomy local kind workers still show scheduling pressure. See [runbooks/overprovisioned.md](runbooks/overprovisioned.md).
